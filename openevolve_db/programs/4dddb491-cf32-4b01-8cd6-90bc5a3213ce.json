{"id": "4dddb491-cf32-4b01-8cd6-90bc5a3213ce", "code": "\"\"\"Evolvable cache replacement policy starting from LRU baseline.\"\"\"\n\nfrom collections import OrderedDict\n\n\nclass EvolvedCache:\n    \"\"\"Byte-based cache with evolvable replacement policy.\"\"\"\n    \n    # EVOLVE-BLOCK-START\n    def __init__(self, capacity_bytes):\n        self.capacity = capacity_bytes\n        self.current_size = 0\n        self.hits = 0\n        self.misses = 0\n        self.cache = OrderedDict()  # obj_id -> (obj_size, freq)\n        self.freq = {}  # obj_id -> access count (persists across evictions)\n\n    def access(self, obj_id, obj_size):\n        \"\"\"Process an access request. Returns True for hit, False for miss.\"\"\"\n        if obj_id in self.cache:\n            size, f = self.cache[obj_id]\n            new_f = min(f + 1, 31)  # cap frequency to limit staleness\n            self.freq[obj_id] = new_f\n            self.cache[obj_id] = (size, new_f)\n            self.cache.move_to_end(obj_id)\n            self.hits += 1\n            return True\n\n        # Miss\n        self.misses += 1\n        f = self.freq.get(obj_id, 0) + 1\n        self.freq[obj_id] = f\n\n        # Skip objects larger than cache capacity\n        if obj_size > self.capacity:\n            return False\n\n        # Compute value of incoming object: freq / size (higher = more valuable)\n        incoming_value = f / obj_size\n\n        # Admission: compare incoming value against worst cached item\n        if self.current_size + obj_size > self.capacity and self.cache:\n            # Find the worst item (lowest value = size/freq is highest)\n            worst_score = -1\n            worst_id = None\n            for eid, (esz, ef) in self.cache.items():\n                score = esz / ef  # high score = bad (large, infrequent)\n                if score > worst_score:\n                    worst_score = score\n                    worst_id = eid\n            worst_esz, worst_ef = self.cache[worst_id]\n            worst_value = worst_ef / worst_esz\n            # Only admit if incoming is more valuable than worst cached item\n            if incoming_value < worst_value:\n                return False\n\n        # Evict: score = size / freq, full scan for best candidate\n        while self.current_size + obj_size > self.capacity and self.cache:\n            best_score = -1\n            best_id = None\n            for eid, (esz, ef) in self.cache.items():\n                score = esz / ef\n                if score > best_score:\n                    best_score = score\n                    best_id = eid\n            if best_id is None:\n                best_id = next(iter(self.cache))\n            esz, _ = self.cache.pop(best_id)\n            self.current_size -= esz\n\n        # Insert new object\n        self.cache[obj_id] = (obj_size, f)\n        self.current_size += obj_size\n        return False\n    # EVOLVE-BLOCK-END\n    \n    def hit_rate(self):\n        \"\"\"Return the hit rate as a fraction.\"\"\"\n        total = self.hits + self.misses\n        return self.hits / total if total > 0 else 0.0\n", "changes_description": null, "language": "python", "parent_id": "9d0a6b7d-0c2c-47da-9542-73b0f54b09cb", "generation": 6, "timestamp": 1780939302.4821734, "iteration_found": 23, "metrics": {"score": 0.55396, "hit_rate": 0.55396, "time": 24.921292543411255, "combined_score": 0.55396, "artifacts": {"final_size_bytes": 16777216, "final_size_mb": 16.0, "capacity_mb": 16.0, "total_hits": 27698, "total_misses": 22302, "cache_items": 6087, "time_factor": 1.0}}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  def access(self, obj_id, obj_size):\n          \"\"\"Process an access request. Returns True for hit, False for miss.\"\"\"\n          if obj_id in self.cache:\n              size, f = self.cache[obj_id]\n              new_f = min(f + 1, 15)  # cap frequency to limit staleness\n              self.freq[obj_id] = new_f\n              self.cache[obj_id] = (size, new_f)\n              self.cache.move_to_end(obj_id)\n              self.hits += 1\n              return True\n  \n          # Miss\n          self.misses += 1\n          f = self.freq.get(obj_id, 0) + 1\n          self.freq[obj_id] = f\n  \n          # Skip objects larger than cache capacity\n          if obj_size > self.capacity:\n              return False\n  \n          # Admission: block first-time objects when cache is full\n          if f == 1 and self.current_size + obj_size > self.capacity:\n              return False\n  \n          # Evict: score = size / freq, full scan for best candidate\n          while self.current_size + obj_size > self.capacity and self.cache:\n              best_score = -1\n              best_id = None\n              for eid, (esz, ef) in self.cache.items():\n                  score = esz / ef\n  ... (12 more lines)\nwith:\n  def access(self, obj_id, obj_size):\n          \"\"\"Process an access request. Returns True for hit, False for miss.\"\"\"\n          if obj_id in self.cache:\n              size, f = self.cache[obj_id]\n              new_f = min(f + 1, 31)  # cap frequency to limit staleness\n              self.freq[obj_id] = new_f\n              self.cache[obj_id] = (size, new_f)\n              self.cache.move_to_end(obj_id)\n              self.hits += 1\n              return True\n  \n          # Miss\n          self.misses += 1\n          f = self.freq.get(obj_id, 0) + 1\n          self.freq[obj_id] = f\n  \n          # Skip objects larger than cache capacity\n          if obj_size > self.capacity:\n              return False\n  \n          # Compute value of incoming object: freq / size (higher = more valuable)\n          incoming_value = f / obj_size\n  \n          # Admission: compare incoming value against worst cached item\n          if self.current_size + obj_size > self.capacity and self.cache:\n              # Find the worst item (lowest value = size/freq is highest)\n              worst_score = -1\n              worst_id = None\n              for eid, (esz, ef) in self.cache.items():\n                  score = esz / ef  # high score = bad (large, infrequent)\n  ... (27 more lines)", "parent_metrics": {"score": 0.52842, "hit_rate": 0.52842, "time": 2.3116092681884766, "combined_score": 0.52842, "artifacts": {"final_size_bytes": 16755712, "final_size_mb": 15.98, "capacity_mb": 16.0, "total_hits": 26421, "total_misses": 23579, "cache_items": 4237, "time_factor": 1.0}}, "island": 1}, "prompts": null, "artifacts_json": null, "artifact_dir": null, "embedding": null}