{"id": "4ced5964-0162-4a63-bb3b-43d04e697d12", "code": "\"\"\"Evolvable cache replacement policy starting from LRU baseline.\"\"\"\n\nfrom collections import OrderedDict\n\n\nclass EvolvedCache:\n    \"\"\"Byte-based cache with evolvable replacement policy.\"\"\"\n    \n    # EVOLVE-BLOCK-START\n    def __init__(self, capacity_bytes):\n        self.capacity = capacity_bytes\n        self.current_size = 0\n        self.hits = 0\n        self.misses = 0\n        self.cache = OrderedDict()  # obj_id -> (obj_size, freq)\n        self.freq = {}  # obj_id -> access count (persists across evictions)\n        self.age = 0  # global access counter for recency\n        self.last_access = {}  # obj_id -> age at last access\n    \n    def access(self, obj_id, obj_size):\n        \"\"\"Process an access request. Returns True for hit, False for miss.\"\"\"\n        self.age += 1\n        if obj_id in self.cache:\n            size, f = self.cache[obj_id]\n            new_f = f + 1\n            self.freq[obj_id] = new_f\n            self.last_access[obj_id] = self.age\n            self.cache[obj_id] = (size, new_f)\n            self.cache.move_to_end(obj_id)\n            self.hits += 1\n            return True\n        \n        # Miss\n        self.misses += 1\n        f = self.freq.get(obj_id, 0) + 1\n        self.freq[obj_id] = f\n        self.last_access[obj_id] = self.age\n        \n        # Skip objects larger than cache capacity\n        if obj_size > self.capacity:\n            return False\n\n        # Evict: score = size * age_gap / freq (prefer evicting old, large, infrequent)\n        while self.current_size + obj_size > self.capacity and self.cache:\n            best_score = -1\n            best_id = None\n            checked = 0\n            for eid, (esz, ef) in self.cache.items():\n                age_gap = self.age - self.last_access.get(eid, 0) + 1\n                score = (esz * age_gap) / ef\n                if score > best_score:\n                    best_score = score\n                    best_id = eid\n                checked += 1\n                if checked >= 32:\n                    break\n            if best_id is None:\n                best_id = next(iter(self.cache))\n            esz, _ = self.cache.pop(best_id)\n            self.current_size -= esz\n        \n        # Insert new object\n        self.cache[obj_id] = (obj_size, f)\n        self.current_size += obj_size\n        return False\n    # EVOLVE-BLOCK-END\n    \n    def hit_rate(self):\n        \"\"\"Return the hit rate as a fraction.\"\"\"\n        total = self.hits + self.misses\n        return self.hits / total if total > 0 else 0.0\n", "changes_description": null, "language": "python", "parent_id": "986ecde4-c2ca-40e5-b1c1-bae2b7d833a9", "generation": 3, "timestamp": 1780938925.5816586, "iteration_found": 8, "metrics": {"score": 0.506352, "hit_rate": 0.48224, "time": 0.3869516849517822, "combined_score": 0.506352, "artifacts": {"final_size_bytes": 16749056, "final_size_mb": 15.97, "capacity_mb": 16.0, "total_hits": 24112, "total_misses": 25888, "cache_items": 1577, "time_factor": 1.05}}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  # Admission filter: only admit if seen before (freq >= 2) OR cache has space\n          # This implements a simple TinyLFU-style admission gate\n          if f < 2 and self.current_size + obj_size > self.capacity:\n              return False\n  \n          # Evict: use size-weighted score = size / freq to pick best eviction candidate\n          # Higher score = more bytes per access = better to evict\n          while self.current_size + obj_size > self.capacity and self.cache:\n              best_score = -1\n              best_id = None\n              checked = 0\n              for eid, (esz, ef) in self.cache.items():\n                  # score: bytes freed per unit of frequency lost\n                  score = esz / ef\n                  if score > best_score:\n                      best_score = score\n                      best_id = eid\n                  checked += 1\n                  if checked >= 16:\n                      break\n              if best_id is None:\n                  best_id = next(iter(self.cache))\n              esz, _ = self.cache.pop(best_id)\n              self.current_size -= esz\nwith:\n  # Evict: use size-weighted score combining recency and frequency\n          while self.current_size + obj_size > self.capacity and self.cache:\n              best_score = -1\n              best_id = None\n              checked = 0\n              total = len(self.cache)\n              pos = 0\n              for eid, (esz, ef) in self.cache.items():\n                  # recency_penalty: items near front (LRU end) get higher score\n                  recency = 1.0 - pos / max(total, 1)\n                  # score: size * recency / freq => prefer evicting large, old, infrequent\n                  score = (esz * (1.0 + recency)) / ef\n                  if score > best_score:\n                      best_score = score\n                      best_id = eid\n                  pos += 1\n                  checked += 1\n                  if checked >= 32:\n                      break\n              if best_id is None:\n                  best_id = next(iter(self.cache))\n              esz, _ = self.cache.pop(best_id)\n              self.current_size -= esz\nChange 2: Replace:\n  # Skip objects larger than cache capacity\n          if obj_size > self.capacity:\n              return False\n  \n          # Evict: use size-weighted score combining recency and frequency\nwith:\n  # Skip objects larger than cache capacity\n          if obj_size > self.capacity:\n              return False\n  \n          # Admission: if cache is full, only admit if new object beats worst cached item\n          if self.current_size + obj_size > self.capacity and self.cache:\n              # Find worst item in LRU portion (first few items)\n              worst_score = -1\n              worst_id = None\n              checked = 0\n              for eid, (esz, ef) in self.cache.items():\n                  score = esz / ef\n                  if score > worst_score:\n                      worst_score = score\n                      worst_id = eid\n                  checked += 1\n                  if checked >= 8:\n                      break\n              # New object score (size=obj_size, freq=f)\n              new_score = obj_size / f\n              if new_score >= worst_score and f < 2:\n                  return False\n  \n          # Evict: use size-weighted score combining recency and frequency\nChange 3: Replace:\n  def __init__(self, capacity_bytes):\n          self.capacity = capacity_bytes\n          self.current_size = 0\n          self.hits = 0\n          self.misses = 0\n          self.cache = OrderedDict()  # obj_id -> (obj_size, freq)\n          self.freq = {}  # obj_id -> access count (persists across evictions)\nwith:\n  def __init__(self, capacity_bytes):\n          self.capacity = capacity_bytes\n          self.current_size = 0\n          self.hits = 0\n          self.misses = 0\n          self.cache = OrderedDict()  # obj_id -> (obj_size, freq)\n          self.freq = {}  # obj_id -> access count (persists across evictions)\n          self.age = 0  # global access counter for recency\n          self.last_access = {}  # obj_id -> age at last access\nChange 4: Replace:\n  def access(self, obj_id, obj_size):\n          \"\"\"Process an access request. Returns True for hit, False for miss.\"\"\"\n          if obj_id in self.cache:\n              size, f = self.cache[obj_id]\n              new_f = f + 1\n              self.freq[obj_id] = new_f\n              self.cache[obj_id] = (size, new_f)\n              self.cache.move_to_end(obj_id)\n              self.hits += 1\n              return True\n  \n          # Miss\n          self.misses += 1\n          f = self.freq.get(obj_id, 0) + 1\n          self.freq[obj_id] = f\n  \n          # Skip objects larger than cache capacity\n          if obj_size > self.capacity:\n              return False\n  \n          # Admission: if cache is full, only admit if new object beats worst cached item\n          if self.current_size + obj_size > self.capacity and self.cache:\n              # Find worst item in LRU portion (first few items)\n              worst_score = -1\n              worst_id = None\n              checked = 0\n              for eid, (esz, ef) in self.cache.items():\n                  score = esz / ef\n                  if score > worst_score:\n                      worst_score = score\n  ... (37 more lines)\nwith:\n  def access(self, obj_id, obj_size):\n          \"\"\"Process an access request. Returns True for hit, False for miss.\"\"\"\n          self.age += 1\n          if obj_id in self.cache:\n              size, f = self.cache[obj_id]\n              new_f = f + 1\n              self.freq[obj_id] = new_f\n              self.last_access[obj_id] = self.age\n              self.cache[obj_id] = (size, new_f)\n              self.cache.move_to_end(obj_id)\n              self.hits += 1\n              return True\n  \n          # Miss\n          self.misses += 1\n          f = self.freq.get(obj_id, 0) + 1\n          self.freq[obj_id] = f\n          self.last_access[obj_id] = self.age\n  \n          # Skip objects larger than cache capacity\n          if obj_size > self.capacity:\n              return False\n  \n          # Evict: score = size * age_gap / freq (prefer evicting old, large, infrequent)\n          while self.current_size + obj_size > self.capacity and self.cache:\n              best_score = -1\n              best_id = None\n              checked = 0\n              for eid, (esz, ef) in self.cache.items():\n                  age_gap = self.age - self.last_access.get(eid, 0) + 1\n  ... (16 more lines)", "parent_metrics": {"score": 0.518238, "hit_rate": 0.49356, "time": 0.06638240814208984, "combined_score": 0.518238, "artifacts": {"final_size_bytes": 16776192, "final_size_mb": 16.0, "capacity_mb": 16.0, "total_hits": 24678, "total_misses": 25322, "cache_items": 2565, "time_factor": 1.05}}, "island": 1}, "prompts": null, "artifacts_json": null, "artifact_dir": null, "embedding": null}