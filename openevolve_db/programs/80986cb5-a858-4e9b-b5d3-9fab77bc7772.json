{"id": "80986cb5-a858-4e9b-b5d3-9fab77bc7772", "code": "\"\"\"Evolvable cache replacement policy starting from LRU baseline.\"\"\"\n\nfrom collections import OrderedDict\n\n\nclass EvolvedCache:\n    \"\"\"Byte-based cache with evolvable replacement policy.\"\"\"\n    \n    # EVOLVE-BLOCK-START\n    def __init__(self, capacity_bytes):\n        self.capacity = capacity_bytes\n        self.current_size = 0\n        self.hits = 0\n        self.misses = 0\n        self.cache = OrderedDict()  # obj_id -> (obj_size, freq)\n        self.freq = {}  # obj_id -> access count\n        self.ghost = {}  # recently evicted: obj_id -> evict_count (for re-admission boost)\n        self._evict_count = 0\n\n    def access(self, obj_id, obj_size):\n        \"\"\"Process a cache access. Returns True for hit, False for miss.\"\"\"\n        f = self.freq.get(obj_id, 0) + 1\n        self.freq[obj_id] = f\n\n        if obj_id in self.cache:\n            sz, _ = self.cache[obj_id]\n            self.cache[obj_id] = (sz, f)\n            self.cache.move_to_end(obj_id)\n            self.hits += 1\n            return True\n\n        self.misses += 1\n\n        if obj_size > self.capacity:\n            return False\n\n        # Ghost hit: item was recently evicted, treat as higher priority\n        in_ghost = obj_id in self.ghost\n\n        # Admission filter: block low-freq items when cache is nearly full\n        if self.current_size > self.capacity * 0.85 and not in_ghost:\n            if f == 1:\n                return False\n            if f == 2 and obj_size > self.capacity * 0.004:\n                return False\n            if f == 3 and obj_size > self.capacity * 0.02:\n                return False\n\n        # Evict: scan front items, pick lowest freq/size score\n        while self.current_size + obj_size > self.capacity and self.cache:\n            best_id = None\n            best_score = float('inf')\n            for i, (cid, (csz, cf)) in enumerate(self.cache.items()):\n                score = cf / (csz ** 0.1)\n                if score < best_score:\n                    best_score = score\n                    best_id = cid\n                if i >= 35:\n                    break\n            if best_id is None:\n                best_id = next(iter(self.cache))\n            esz, _ = self.cache.pop(best_id)\n            self.current_size -= esz\n            # Track in ghost cache\n            self._evict_count += 1\n            self.ghost[best_id] = self._evict_count\n\n        # Prune ghost cache\n        if len(self.ghost) > 8000:\n            cutoff = self._evict_count - 6000\n            self.ghost = {k: v for k, v in self.ghost.items() if v > cutoff}\n\n        # Prune freq table periodically\n        if len(self.freq) > 60000:\n            in_cache = set(self.cache.keys())\n            extras = [k for k in self.freq if k not in in_cache]\n            for k in extras[:30000]:\n                del self.freq[k]\n\n        self.cache[obj_id] = (obj_size, f)\n        self.current_size += obj_size\n        return False\n    # EVOLVE-BLOCK-END\n    \n    def hit_rate(self):\n        \"\"\"Return the hit rate as a fraction.\"\"\"\n        total = self.hits + self.misses\n        return self.hits / total if total > 0 else 0.0\n", "changes_description": null, "language": "python", "parent_id": "c9d250b3-ee1d-4ce2-9069-6262c2e8b28d", "generation": 4, "timestamp": 1780939869.464693, "iteration_found": 46, "metrics": {"score": 0.515844, "hit_rate": 0.49128, "time": 0.09150004386901855, "combined_score": 0.515844, "artifacts": {"final_size_bytes": 16756224, "final_size_mb": 15.98, "capacity_mb": 16.0, "total_hits": 24564, "total_misses": 25436, "cache_items": 2423, "time_factor": 1.05}}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  def __init__(self, capacity_bytes):\n          self.capacity = capacity_bytes\n          self.current_size = 0\n          self.hits = 0\n          self.misses = 0\n          self.cache = OrderedDict()  # obj_id -> (obj_size, freq)\n          self.freq = {}  # obj_id -> access count\n  \n      def access(self, obj_id, obj_size):\n          \"\"\"Process a cache access. Returns True for hit, False for miss.\"\"\"\n          f = self.freq.get(obj_id, 0) + 1\n          self.freq[obj_id] = f\n  \n          if obj_id in self.cache:\n              sz, _ = self.cache[obj_id]\n              self.cache[obj_id] = (sz, f)\n              self.cache.move_to_end(obj_id)\n              self.hits += 1\n              return True\n  \n          self.misses += 1\n  \n          if obj_size > self.capacity:\n              return False\n  \n          # Admission filter: block low-freq items when cache is nearly full\n          if self.current_size > self.capacity * 0.85:\n              if f == 1:\n                  return False\n              if f == 2 and obj_size > self.capacity * 0.004:\n  ... (30 more lines)\nwith:\n  def __init__(self, capacity_bytes):\n          self.capacity = capacity_bytes\n          self.current_size = 0\n          self.hits = 0\n          self.misses = 0\n          self.cache = OrderedDict()  # obj_id -> (obj_size, freq)\n          self.freq = {}  # obj_id -> access count\n          self.ghost = {}  # recently evicted: obj_id -> evict_count (for re-admission boost)\n          self._evict_count = 0\n  \n      def access(self, obj_id, obj_size):\n          \"\"\"Process a cache access. Returns True for hit, False for miss.\"\"\"\n          f = self.freq.get(obj_id, 0) + 1\n          self.freq[obj_id] = f\n  \n          if obj_id in self.cache:\n              sz, _ = self.cache[obj_id]\n              self.cache[obj_id] = (sz, f)\n              self.cache.move_to_end(obj_id)\n              self.hits += 1\n              return True\n  \n          self.misses += 1\n  \n          if obj_size > self.capacity:\n              return False\n  \n          # Ghost hit: item was recently evicted, treat as higher priority\n          in_ghost = obj_id in self.ghost\n  \n  ... (43 more lines)", "parent_metrics": {"score": 0.515844, "hit_rate": 0.49128, "time": 0.07825994491577148, "combined_score": 0.515844, "artifacts": {"final_size_bytes": 16756224, "final_size_mb": 15.98, "capacity_mb": 16.0, "total_hits": 24564, "total_misses": 25436, "cache_items": 2423, "time_factor": 1.05}}, "island": 0}, "prompts": null, "artifacts_json": null, "artifact_dir": null, "embedding": null}