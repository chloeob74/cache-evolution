{"id": "86d1f5cc-e51e-4198-9f6e-b678c8bf3134", "code": "\"\"\"Evolvable cache replacement policy starting from LRU baseline.\"\"\"\n\nfrom collections import OrderedDict\n\n\nclass EvolvedCache:\n    \"\"\"Byte-based cache with evolvable replacement policy.\"\"\"\n    \n    # EVOLVE-BLOCK-START\n    def __init__(self, capacity_bytes):\n        self.capacity = capacity_bytes\n        self.current_size = 0\n        self.hits = 0\n        self.misses = 0\n        self.cache = OrderedDict()  # obj_id -> (obj_size, freq, priority)\n        self.freq = {}  # obj_id -> access count (persists across evictions)\n        self.clock = 0  # GDSF clock value\n\n    def access(self, obj_id, obj_size):\n        \"\"\"Process an access request. Returns True for hit, False for miss.\"\"\"\n        if obj_id in self.cache:\n            size, f, _ = self.cache[obj_id]\n            new_f = min(f + 1, 15)\n            self.freq[obj_id] = new_f\n            # Update priority: clock + freq/size (GDSF-style)\n            new_priority = self.clock + new_f / size\n            self.cache[obj_id] = (size, new_f, new_priority)\n            self.cache.move_to_end(obj_id)\n            self.hits += 1\n            return True\n\n        # Miss\n        self.misses += 1\n        f = self.freq.get(obj_id, 0) + 1\n        self.freq[obj_id] = f\n\n        if obj_size > self.capacity:\n            return False\n\n        # Admission: block first-time objects when cache is full\n        if f == 1 and self.current_size + obj_size > self.capacity:\n            return False\n\n        # Evict: lowest GDSF priority first (clock + freq/size)\n        while self.current_size + obj_size > self.capacity and self.cache:\n            best_id = None\n            best_pri = float('inf')\n            for eid, (esz, ef, epri) in self.cache.items():\n                if epri < best_pri:\n                    best_pri = epri\n                    best_id = eid\n            if best_id is None:\n                best_id = next(iter(self.cache))\n            else:\n                self.clock = best_pri  # advance clock\n            esz, _, _ = self.cache.pop(best_id)\n            self.current_size -= esz\n\n        priority = self.clock + f / obj_size\n        self.cache[obj_id] = (obj_size, f, priority)\n        self.current_size += obj_size\n        return False\n    # EVOLVE-BLOCK-END\n    \n    def hit_rate(self):\n        \"\"\"Return the hit rate as a fraction.\"\"\"\n        total = self.hits + self.misses\n        return self.hits / total if total > 0 else 0.0\n", "changes_description": null, "language": "python", "parent_id": "9b948282-b329-4195-8f60-984a74af78d3", "generation": 6, "timestamp": 1780939567.5436518, "iteration_found": 35, "metrics": {"score": 0.557466, "hit_rate": 0.53092, "time": 1.5879573822021484, "combined_score": 0.557466, "artifacts": {"final_size_bytes": 16776192, "final_size_mb": 16.0, "capacity_mb": 16.0, "total_hits": 26546, "total_misses": 23454, "cache_items": 4025, "time_factor": 1.05}}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  def __init__(self, capacity_bytes):\n          self.capacity = capacity_bytes\n          self.current_size = 0\n          self.hits = 0\n          self.misses = 0\n          self.cache = OrderedDict()  # obj_id -> (obj_size, freq)\n          self.freq = {}  # obj_id -> access count (persists across evictions)\n  \n      def access(self, obj_id, obj_size):\n          \"\"\"Process an access request. Returns True for hit, False for miss.\"\"\"\n          if obj_id in self.cache:\n              size, f = self.cache[obj_id]\n              new_f = min(f + 1, 15)\n              self.freq[obj_id] = new_f\n              self.cache[obj_id] = (size, new_f)\n              self.cache.move_to_end(obj_id)\n              self.hits += 1\n              return True\n  \n          # Miss\n          self.misses += 1\n          f = self.freq.get(obj_id, 0) + 1\n          self.freq[obj_id] = f\n  \n          # Skip objects larger than cache capacity\n          if obj_size > self.capacity:\n              return False\n  \n          # Admission: block first-time objects when cache is full\n          if f == 1 and self.current_size + obj_size > self.capacity:\n  ... (20 more lines)\nwith:\n  def __init__(self, capacity_bytes):\n          self.capacity = capacity_bytes\n          self.current_size = 0\n          self.hits = 0\n          self.misses = 0\n          self.cache = OrderedDict()  # obj_id -> (obj_size, freq, priority)\n          self.freq = {}  # obj_id -> access count (persists across evictions)\n          self.clock = 0  # GDSF clock value\n  \n      def access(self, obj_id, obj_size):\n          \"\"\"Process an access request. Returns True for hit, False for miss.\"\"\"\n          if obj_id in self.cache:\n              size, f, _ = self.cache[obj_id]\n              new_f = min(f + 1, 15)\n              self.freq[obj_id] = new_f\n              # Update priority: clock + freq/size (GDSF-style)\n              new_priority = self.clock + new_f / size\n              self.cache[obj_id] = (size, new_f, new_priority)\n              self.cache.move_to_end(obj_id)\n              self.hits += 1\n              return True\n  \n          # Miss\n          self.misses += 1\n          f = self.freq.get(obj_id, 0) + 1\n          self.freq[obj_id] = f\n  \n          if obj_size > self.capacity:\n              return False\n  \n  ... (23 more lines)", "parent_metrics": {"score": 0.554841, "hit_rate": 0.52842, "time": 1.9897456169128418, "combined_score": 0.554841, "artifacts": {"final_size_bytes": 16755712, "final_size_mb": 15.98, "capacity_mb": 16.0, "total_hits": 26421, "total_misses": 23579, "cache_items": 4237, "time_factor": 1.05}}, "island": 1}, "prompts": null, "artifacts_json": null, "artifact_dir": null, "embedding": null}