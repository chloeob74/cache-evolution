{"id": "133075bf-a317-4c48-a74b-4c2edb69e214", "code": "\"\"\"Evolvable cache replacement policy starting from LRU baseline.\"\"\"\n\nfrom collections import OrderedDict\n\n\nclass EvolvedCache:\n    \"\"\"Byte-based cache with evolvable replacement policy.\"\"\"\n    \n    # EVOLVE-BLOCK-START\n    def __init__(self, capacity_bytes):\n        self.capacity = capacity_bytes\n        self.current_size = 0\n        self.hits = 0\n        self.misses = 0\n        # ARC: T1 (recency) + T2 (frequency) + ghost lists B1, B2\n        self.t1 = OrderedDict()  # recently accessed once\n        self.t2 = OrderedDict()  # accessed more than once (hot)\n        self.b1 = OrderedDict()  # ghost of evicted T1: obj_id -> size\n        self.b2 = OrderedDict()  # ghost of evicted T2: obj_id -> size\n        self.t1_size = 0\n        self.t2_size = 0\n        self.t1_sz = {}\n        self.t2_sz = {}\n        self.p = 0  # target bytes for T1\n        self.ghost_max = 4000  # larger ghost for better adaptation signal\n        self.total_obj = 0  # total objects inserted (for avg size estimate)\n        self.total_bytes = 0  # total bytes inserted\n\n    def _evict_one(self):\n        \"\"\"Evict one item using ARC policy.\"\"\"\n        if self.t1 and (self.t1_size >= max(1, self.p) or not self.t2):\n            eid, _ = self.t1.popitem(last=False)\n            esz = self.t1_sz.pop(eid)\n            self.t1_size -= esz\n            self.current_size -= esz\n            self.b1[eid] = esz\n            if len(self.b1) > self.ghost_max:\n                self.b1.popitem(last=False)\n        elif self.t2:\n            eid, _ = self.t2.popitem(last=False)\n            esz = self.t2_sz.pop(eid)\n            self.t2_size -= esz\n            self.current_size -= esz\n            self.b2[eid] = esz\n            if len(self.b2) > self.ghost_max:\n                self.b2.popitem(last=False)\n        elif self.t1:\n            eid, _ = self.t1.popitem(last=False)\n            esz = self.t1_sz.pop(eid)\n            self.t1_size -= esz\n            self.current_size -= esz\n            self.b1[eid] = esz\n            if len(self.b1) > self.ghost_max:\n                self.b1.popitem(last=False)\n\n    def _evict_to_fit(self, obj_size):\n        while self.current_size + obj_size > self.capacity and (self.t1 or self.t2):\n            self._evict_one()\n\n    def access(self, obj_id, obj_size):\n        \"\"\"Process an access request. Returns True for hit, False for miss.\"\"\"\n        # Hit in T1 -> promote to T2\n        if obj_id in self.t1_sz:\n            sz = self.t1_sz.pop(obj_id)\n            self.t1.pop(obj_id)\n            self.t1_size -= sz\n            self.current_size -= sz\n            self.t2[obj_id] = 1\n            self.t2_sz[obj_id] = sz\n            self.t2_size += sz\n            self.current_size += sz\n            self.hits += 1\n            return True\n\n        # Hit in T2 -> move to MRU\n        if obj_id in self.t2_sz:\n            self.t2.move_to_end(obj_id)\n            self.hits += 1\n            return True\n\n        self.misses += 1\n        if obj_size > self.capacity:\n            return False\n\n        # Compute avg size for normalized adaptation\n        avg_sz = max(1, self.total_bytes // max(1, self.total_obj))\n        self.total_obj += 1\n        self.total_bytes += obj_size\n\n        # Ghost hit in B1: favor recency, increase p\n        if obj_id in self.b1:\n            ghost_sz = self.b1.pop(obj_id)\n            # Normalize delta by avg object size for stable adaptation\n            delta = max(avg_sz, ghost_sz)\n            self.p = min(self.p + delta, self.capacity)\n            self._evict_to_fit(obj_size)\n            self.t2[obj_id] = 1\n            self.t2_sz[obj_id] = obj_size\n            self.t2_size += obj_size\n            self.current_size += obj_size\n            return False\n\n        # Ghost hit in B2: favor frequency, decrease p\n        if obj_id in self.b2:\n            ghost_sz = self.b2.pop(obj_id)\n            delta = max(avg_sz, ghost_sz)\n            self.p = max(self.p - delta, 0)\n            self._evict_to_fit(obj_size)\n            self.t2[obj_id] = 1\n            self.t2_sz[obj_id] = obj_size\n            self.t2_size += obj_size\n            self.current_size += obj_size\n            return False\n\n        # Cold miss -> admit to T1\n        self._evict_to_fit(obj_size)\n        self.t1[obj_id] = 1\n        self.t1_sz[obj_id] = obj_size\n        self.t1_size += obj_size\n        self.current_size += obj_size\n        return False\n    # EVOLVE-BLOCK-END\n    \n    def hit_rate(self):\n        \"\"\"Return the hit rate as a fraction.\"\"\"\n        total = self.hits + self.misses\n        return self.hits / total if total > 0 else 0.0\n", "changes_description": null, "language": "python", "parent_id": "25a19aa1-e938-49c8-96c9-68701a8c7536", "generation": 5, "timestamp": 1780939682.4119058, "iteration_found": 39, "metrics": {"score": 0.530019, "hit_rate": 0.50478, "time": 0.08228039741516113, "combined_score": 0.530019, "artifacts": {"final_size_bytes": 16752640, "final_size_mb": 15.98, "capacity_mb": 16.0, "total_hits": 25239, "total_misses": 24761, "time_factor": 1.05}}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  def __init__(self, capacity_bytes):\n          self.capacity = capacity_bytes\n          self.current_size = 0\n          self.hits = 0\n          self.misses = 0\n          # Segmented LRU: probationary (new) and protected (hot) segments\n          # Protected segment holds ~75% of capacity, probationary ~25%\n          self.protected = OrderedDict()   # obj_id -> obj_size (hot items)\n          self.probationary = OrderedDict()  # obj_id -> obj_size (new/cold items)\n          self.protected_size = 0\n          self.probationary_size = 0\n          self.protected_cap = int(capacity_bytes * 0.75)\n  \n      def _evict_to_fit(self, obj_size):\n          \"\"\"Evict items until obj_size bytes are free.\"\"\"\n          while self.current_size + obj_size > self.capacity:\n              if self.probationary:\n                  eid, esz = self.probationary.popitem(last=False)\n                  self.probationary_size -= esz\n                  self.current_size -= esz\n              elif self.protected:\n                  eid, esz = self.protected.popitem(last=False)\n                  self.protected_size -= esz\n                  self.current_size -= esz\n              else:\n                  break\n  \n      def access(self, obj_id, obj_size):\n          \"\"\"Process an access request. Returns True for hit, False for miss.\"\"\"\n          if obj_id in self.protected:\n  ... (32 more lines)\nwith:\n  def __init__(self, capacity_bytes):\n          self.capacity = capacity_bytes\n          self.current_size = 0\n          self.hits = 0\n          self.misses = 0\n          # ARC: T1 (recency) + T2 (frequency) + ghost lists B1, B2\n          self.t1 = OrderedDict()  # recently accessed once\n          self.t2 = OrderedDict()  # accessed more than once (hot)\n          self.b1 = OrderedDict()  # ghost of evicted T1: obj_id -> size\n          self.b2 = OrderedDict()  # ghost of evicted T2: obj_id -> size\n          self.t1_size = 0\n          self.t2_size = 0\n          self.t1_sz = {}\n          self.t2_sz = {}\n          self.p = 0  # target bytes for T1\n          self.ghost_max = 4000  # larger ghost for better adaptation signal\n          self.total_obj = 0  # total objects inserted (for avg size estimate)\n          self.total_bytes = 0  # total bytes inserted\n  \n      def _evict_one(self):\n          \"\"\"Evict one item using ARC policy.\"\"\"\n          if self.t1 and (self.t1_size >= max(1, self.p) or not self.t2):\n              eid, _ = self.t1.popitem(last=False)\n              esz = self.t1_sz.pop(eid)\n              self.t1_size -= esz\n              self.current_size -= esz\n              self.b1[eid] = esz\n              if len(self.b1) > self.ghost_max:\n                  self.b1.popitem(last=False)\n          elif self.t2:\n  ... (82 more lines)", "parent_metrics": {"score": 0.528822, "hit_rate": 0.50364, "time": 0.04294538497924805, "combined_score": 0.528822, "artifacts": {"final_size_bytes": 16755200, "final_size_mb": 15.98, "capacity_mb": 16.0, "total_hits": 25182, "total_misses": 24818, "protected_items": 1699, "protected_size_mb": 11.38, "time_factor": 1.05}}, "island": 2}, "prompts": null, "artifacts_json": null, "artifact_dir": null, "embedding": null}