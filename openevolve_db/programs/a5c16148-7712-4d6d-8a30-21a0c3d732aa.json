{"id": "a5c16148-7712-4d6d-8a30-21a0c3d732aa", "code": "\"\"\"Evolvable cache replacement policy starting from LRU baseline.\"\"\"\n\nfrom collections import OrderedDict\n\n\nclass EvolvedCache:\n    \"\"\"Byte-based cache with evolvable replacement policy.\"\"\"\n    \n    # EVOLVE-BLOCK-START\n    def __init__(self, capacity_bytes):\n        self.capacity = capacity_bytes\n        self.current_size = 0\n        self.hits = 0\n        self.misses = 0\n        self.cache = OrderedDict()  # obj_id -> (obj_size, freq)\n        self.freq = {}  # obj_id -> access count\n    \n    def access(self, obj_id, obj_size):\n        \"\"\"Process a cache access. Returns True for hit, False for miss.\"\"\"\n        f = self.freq.get(obj_id, 0) + 1\n        self.freq[obj_id] = f\n        \n        if obj_id in self.cache:\n            sz, _ = self.cache[obj_id]\n            self.cache[obj_id] = (sz, f)\n            self.cache.move_to_end(obj_id)\n            self.hits += 1\n            return True\n        \n        self.misses += 1\n        \n        if obj_size > self.capacity:\n            return False\n        \n        # Evict: scan items, evict lowest (freq * recency_weight) / size\n        n_items = len(self.cache)\n        window = max(12, n_items // 8)\n        while self.current_size + obj_size > self.capacity and self.cache:\n            best_id = None\n            best_score = float('inf')\n            for i, (cid, (csz, cf)) in enumerate(self.cache.items()):\n                # Position penalty: items near front (LRU) get lower recency bonus\n                recency = 1.0 + i * 0.05\n                score = (cf * recency) / (csz ** 0.2)\n                if score < best_score:\n                    best_score = score\n                    best_id = cid\n                if i >= window:\n                    break\n            if best_id is None:\n                best_id = next(iter(self.cache))\n            esz, _ = self.cache.pop(best_id)\n            self.current_size -= esz\n\n        # Prune freq table periodically\n        if len(self.freq) > 60000:\n            in_cache = set(self.cache.keys())\n            extras = [k for k in self.freq if k not in in_cache]\n            for k in extras[:30000]:\n                del self.freq[k]\n        \n        self.cache[obj_id] = (obj_size, f)\n        self.current_size += obj_size\n        return False\n    # EVOLVE-BLOCK-END\n    \n    def hit_rate(self):\n        \"\"\"Return the hit rate as a fraction.\"\"\"\n        total = self.hits + self.misses\n        return self.hits / total if total > 0 else 0.0\n", "changes_description": null, "language": "python", "parent_id": "a537ee33-7bc9-42a4-8b84-ad25f1777a64", "generation": 2, "timestamp": 1780938971.4680922, "iteration_found": 10, "metrics": {"score": 0.49436, "hit_rate": 0.49436, "time": 2.264033794403076, "combined_score": 0.49436, "artifacts": {"final_size_bytes": 16772096, "final_size_mb": 16.0, "capacity_mb": 16.0, "total_hits": 24718, "total_misses": 25282, "cache_items": 1738, "time_factor": 1.0}}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  # Evict: scan front items, pick lowest freq/size score\n          while self.current_size + obj_size > self.capacity and self.cache:\n              best_id = None\n              best_score = float('inf')\n              for i, (cid, (csz, cf)) in enumerate(self.cache.items()):\n                  score = cf / (csz ** 0.3)\n                  if score < best_score:\n                      best_score = score\n                      best_id = cid\n                  if i >= 5:\n                      break\n              if best_id is None:\n                  best_id = next(iter(self.cache))\n              esz, _ = self.cache.pop(best_id)\n              self.current_size -= esz\n  \n          # Prune freq table periodically\n          if len(self.freq) > 60000:\n              in_cache = set(self.cache.keys())\n              extras = [k for k in self.freq if k not in in_cache]\n              for k in extras[:30000]:\n                  del self.freq[k]\nwith:\n  # Evict: scan items, evict lowest (freq * recency_weight) / size\n          n_items = len(self.cache)\n          window = max(12, n_items // 8)\n          while self.current_size + obj_size > self.capacity and self.cache:\n              best_id = None\n              best_score = float('inf')\n              for i, (cid, (csz, cf)) in enumerate(self.cache.items()):\n                  # Position penalty: items near front (LRU) get lower recency bonus\n                  recency = 1.0 + i * 0.05\n                  score = (cf * recency) / (csz ** 0.2)\n                  if score < best_score:\n                      best_score = score\n                      best_id = cid\n                  if i >= window:\n                      break\n              if best_id is None:\n                  best_id = next(iter(self.cache))\n              esz, _ = self.cache.pop(best_id)\n              self.current_size -= esz\n  \n          # Prune freq table periodically\n          if len(self.freq) > 60000:\n              in_cache = set(self.cache.keys())\n              extras = [k for k in self.freq if k not in in_cache]\n              for k in extras[:30000]:\n                  del self.freq[k]", "parent_metrics": {"score": 0.503622, "hit_rate": 0.47964, "time": 0.1322033405303955, "combined_score": 0.503622, "artifacts": {"final_size_bytes": 16715776, "final_size_mb": 15.94, "capacity_mb": 16.0, "total_hits": 23982, "total_misses": 26018, "cache_items": 1551, "time_factor": 1.05}}, "island": 0}, "prompts": null, "artifacts_json": null, "artifact_dir": null, "embedding": null}