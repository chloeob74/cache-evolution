{"id": "9b948282-b329-4195-8f60-984a74af78d3", "code": "\"\"\"Evolvable cache replacement policy starting from LRU baseline.\"\"\"\n\nfrom collections import OrderedDict\n\n\nclass EvolvedCache:\n    \"\"\"Byte-based cache with evolvable replacement policy.\"\"\"\n    \n    # EVOLVE-BLOCK-START\n    def __init__(self, capacity_bytes):\n        self.capacity = capacity_bytes\n        self.current_size = 0\n        self.hits = 0\n        self.misses = 0\n        self.cache = OrderedDict()  # obj_id -> (obj_size, freq)\n        self.freq = {}  # obj_id -> access count (persists across evictions)\n\n    def access(self, obj_id, obj_size):\n        \"\"\"Process an access request. Returns True for hit, False for miss.\"\"\"\n        if obj_id in self.cache:\n            size, f = self.cache[obj_id]\n            new_f = min(f + 1, 15)\n            self.freq[obj_id] = new_f\n            self.cache[obj_id] = (size, new_f)\n            self.cache.move_to_end(obj_id)\n            self.hits += 1\n            return True\n\n        # Miss\n        self.misses += 1\n        f = self.freq.get(obj_id, 0) + 1\n        self.freq[obj_id] = f\n\n        # Skip objects larger than cache capacity\n        if obj_size > self.capacity:\n            return False\n\n        # Admission: block first-time objects when cache is full\n        if f == 1 and self.current_size + obj_size > self.capacity:\n            return False\n\n        # Evict: score = size / freq, full scan\n        while self.current_size + obj_size > self.capacity and self.cache:\n            best_score = -1\n            best_id = None\n            for eid, (esz, ef) in self.cache.items():\n                score = esz / ef\n                if score > best_score:\n                    best_score = score\n                    best_id = eid\n            if best_id is None:\n                best_id = next(iter(self.cache))\n            esz, _ = self.cache.pop(best_id)\n            self.current_size -= esz\n\n        # Insert new object\n        self.cache[obj_id] = (obj_size, f)\n        self.current_size += obj_size\n        return False\n    # EVOLVE-BLOCK-END\n    \n    def hit_rate(self):\n        \"\"\"Return the hit rate as a fraction.\"\"\"\n        total = self.hits + self.misses\n        return self.hits / total if total > 0 else 0.0\n", "changes_description": null, "language": "python", "parent_id": "a61b067e-871b-4dee-9df2-c9b5ba2ac04a", "generation": 5, "timestamp": 1780939496.5498943, "iteration_found": 32, "metrics": {"score": 0.554841, "hit_rate": 0.52842, "time": 1.9897456169128418, "combined_score": 0.554841, "artifacts": {"final_size_bytes": 16755712, "final_size_mb": 15.98, "capacity_mb": 16.0, "total_hits": 26421, "total_misses": 23579, "cache_items": 4237, "time_factor": 1.05}}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  # Admission: block first-time objects when cache is full\n          if f == 1 and self.current_size + obj_size > self.capacity:\n              return False\n  \n          # Value-based admission for f>=2: only admit if more valuable than worst item\n          if self.current_size + obj_size > self.capacity and self.cache:\n              incoming_value = f / obj_size\n              worst_score = -1\n              worst_id = None\n              for eid, (esz, ef) in self.cache.items():\n                  score = esz / ef\n                  if score > worst_score:\n                      worst_score = score\n                      worst_id = eid\n              if worst_id is not None:\n                  worst_esz, worst_ef = self.cache[worst_id]\n                  if incoming_value < worst_ef / worst_esz:\n                      return False\n  \n          # Evict: score = size / freq, full scan\n          while self.current_size + obj_size > self.capacity and self.cache:\n              best_score = -1\n              best_id = None\n              for eid, (esz, ef) in self.cache.items():\n                  score = esz / ef\n                  if score > best_score:\n                      best_score = score\n                      best_id = eid\n              if best_id is None:\n                  best_id = next(iter(self.cache))\n  ... (2 more lines)\nwith:\n  # Admission: block first-time objects when cache is full\n          if f == 1 and self.current_size + obj_size > self.capacity:\n              return False\n  \n          # Evict: score = size / freq, full scan\n          while self.current_size + obj_size > self.capacity and self.cache:\n              best_score = -1\n              best_id = None\n              for eid, (esz, ef) in self.cache.items():\n                  score = esz / ef\n                  if score > best_score:\n                      best_score = score\n                      best_id = eid\n              if best_id is None:\n                  best_id = next(iter(self.cache))\n              esz, _ = self.cache.pop(best_id)\n              self.current_size -= esz", "parent_metrics": {"score": 0.5181, "hit_rate": 0.5181, "time": 2.351381540298462, "combined_score": 0.5181, "artifacts": {"final_size_bytes": 16777216, "final_size_mb": 16.0, "capacity_mb": 16.0, "total_hits": 25905, "total_misses": 24095, "cache_items": 4190, "time_factor": 1.0}}, "island": 1}, "prompts": null, "artifacts_json": null, "artifact_dir": null, "embedding": null}