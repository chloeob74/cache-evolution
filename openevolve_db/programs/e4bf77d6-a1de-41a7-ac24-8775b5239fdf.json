{"id": "e4bf77d6-a1de-41a7-ac24-8775b5239fdf", "code": "\"\"\"Evolvable cache replacement policy starting from LRU baseline.\"\"\"\n\nfrom collections import OrderedDict\n\n\nclass EvolvedCache:\n    \"\"\"Byte-based cache with evolvable replacement policy.\"\"\"\n    \n    # EVOLVE-BLOCK-START\n    def __init__(self, capacity_bytes):\n        self.capacity = capacity_bytes\n        self.current_size = 0\n        self.hits = 0\n        self.misses = 0\n        self.cache = OrderedDict()  # obj_id -> (obj_size, freq)\n        self.freq = {}  # obj_id -> access count (persists across evictions)\n    \n    def access(self, obj_id, obj_size):\n        \"\"\"Process an access request. Returns True for hit, False for miss.\"\"\"\n        if obj_id in self.cache:\n            size, f = self.cache[obj_id]\n            self.freq[obj_id] = f + 1\n            self.cache[obj_id] = (size, f + 1)\n            self.cache.move_to_end(obj_id)\n            self.hits += 1\n            return True\n        \n        # Miss\n        self.misses += 1\n        f = self.freq.get(obj_id, 0) + 1\n        self.freq[obj_id] = f\n        \n        # Skip objects larger than cache capacity\n        if obj_size > self.capacity:\n            return False\n        \n        # Evict: prefer items with lowest freq, break ties by LRU order\n        while self.current_size + obj_size > self.capacity and self.cache:\n            # Find item with minimum frequency (scan LRU portion)\n            min_freq = None\n            min_id = None\n            checked = 0\n            for eid, (esz, ef) in self.cache.items():\n                if min_freq is None or ef < min_freq:\n                    min_freq = ef\n                    min_id = eid\n                checked += 1\n                if checked >= 8:  # limit scan for speed\n                    break\n            if min_id is None:\n                min_id = next(iter(self.cache))\n            esz, _ = self.cache.pop(min_id)\n            self.current_size -= esz\n        \n        # Insert new object\n        self.cache[obj_id] = (obj_size, f)\n        self.current_size += obj_size\n        return False\n    # EVOLVE-BLOCK-END\n    \n    def hit_rate(self):\n        \"\"\"Return the hit rate as a fraction.\"\"\"\n        total = self.hits + self.misses\n        return self.hits / total if total > 0 else 0.0\n", "changes_description": null, "language": "python", "parent_id": "7e0de3da-ee54-437e-981c-b555c942c858", "generation": 1, "timestamp": 1780938785.174493, "iteration_found": 2, "metrics": {"score": 0.5037480000000001, "hit_rate": 0.47976, "time": 0.09285163879394531, "combined_score": 0.5037480000000001, "artifacts": {"final_size_bytes": 16716800, "final_size_mb": 15.94, "capacity_mb": 16.0, "total_hits": 23988, "total_misses": 26012, "cache_items": 1553, "time_factor": 1.05}}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  def __init__(self, capacity_bytes):\n          self.capacity = capacity_bytes\n          self.current_size = 0\n          self.hits = 0\n          self.misses = 0\n          self.cache = OrderedDict()  # obj_id -> obj_size\n  \n      def access(self, obj_id, obj_size):\n          \"\"\"Process an access request. Returns True for hit, False for miss.\"\"\"\n          if obj_id in self.cache:\n              # Hit: move to end (most recently used)\n              self.cache.move_to_end(obj_id)\n              self.hits += 1\n              return True\n  \n          # Miss\n          self.misses += 1\n  \n          # Skip objects larger than cache capacity\n          if obj_size > self.capacity:\n              return False\n  \n          # Evict until there's room\n          while self.current_size + obj_size > self.capacity and self.cache:\n              evicted_id, evicted_size = self.cache.popitem(last=False)\n              self.current_size -= evicted_size\n  \n          # Insert new object\n          self.cache[obj_id] = obj_size\n          self.current_size += obj_size\n  ... (1 more lines)\nwith:\n  def __init__(self, capacity_bytes):\n          self.capacity = capacity_bytes\n          self.current_size = 0\n          self.hits = 0\n          self.misses = 0\n          self.cache = OrderedDict()  # obj_id -> (obj_size, freq)\n          self.freq = {}  # obj_id -> access count (persists across evictions)\n  \n      def access(self, obj_id, obj_size):\n          \"\"\"Process an access request. Returns True for hit, False for miss.\"\"\"\n          if obj_id in self.cache:\n              size, f = self.cache[obj_id]\n              self.freq[obj_id] = f + 1\n              self.cache[obj_id] = (size, f + 1)\n              self.cache.move_to_end(obj_id)\n              self.hits += 1\n              return True\n  \n          # Miss\n          self.misses += 1\n          f = self.freq.get(obj_id, 0) + 1\n          self.freq[obj_id] = f\n  \n          # Skip objects larger than cache capacity\n          if obj_size > self.capacity:\n              return False\n  \n          # Evict: prefer items with lowest freq, break ties by LRU order\n          while self.current_size + obj_size > self.capacity and self.cache:\n              # Find item with minimum frequency (scan LRU portion)\n  ... (19 more lines)", "parent_metrics": {"score": 0.25161150000000004, "hit_rate": 0.47926, "time": 0.047628164291381836, "combined_score": 0.25161150000000004, "artifacts": {"final_size_bytes": 16771584, "final_size_mb": 15.99, "capacity_mb": 16.0, "total_hits": 23963, "total_misses": 26037, "cache_items": 1547, "time_factor": 0.525}}, "island": 1}, "prompts": null, "artifacts_json": null, "artifact_dir": null, "embedding": null}