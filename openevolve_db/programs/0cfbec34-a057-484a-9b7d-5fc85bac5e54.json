{"id": "0cfbec34-a057-484a-9b7d-5fc85bac5e54", "code": "\"\"\"Evolvable cache replacement policy starting from LRU baseline.\"\"\"\n\nfrom collections import OrderedDict\n\n\nclass EvolvedCache:\n    \"\"\"Byte-based cache with evolvable replacement policy.\"\"\"\n    \n    # EVOLVE-BLOCK-START\n    def __init__(self, capacity_bytes):\n        self.capacity = capacity_bytes\n        self.current_size = 0\n        self.hits = 0\n        self.misses = 0\n        self.cache = OrderedDict()  # obj_id -> (obj_size, freq)\n        self.freq = {}  # obj_id -> access count\n    \n    def access(self, obj_id, obj_size):\n        \"\"\"Process a cache access. Returns True for hit, False for miss.\"\"\"\n        f = self.freq.get(obj_id, 0) + 1\n        self.freq[obj_id] = f\n        \n        if obj_id in self.cache:\n            sz, _ = self.cache[obj_id]\n            self.cache[obj_id] = (sz, f)\n            self.cache.move_to_end(obj_id)\n            self.hits += 1\n            return True\n        \n        self.misses += 1\n        \n        if obj_size > self.capacity:\n            return False\n        \n        # Admission filter: require freq >= 2 for large objects when full\n        if self.current_size > self.capacity * 0.85:\n            if f == 1:\n                return False\n            if f == 2 and obj_size > self.capacity * 0.01:\n                return False\n\n        # Evict: scan front items, pick lowest freq/size score\n        while self.current_size + obj_size > self.capacity and self.cache:\n            best_id = None\n            best_score = float('inf')\n            for i, (cid, (csz, cf)) in enumerate(self.cache.items()):\n                score = cf / (csz ** 0.15)\n                if score < best_score:\n                    best_score = score\n                    best_id = cid\n                if i >= 20:\n                    break\n            if best_id is None:\n                best_id = next(iter(self.cache))\n            esz, _ = self.cache.pop(best_id)\n            self.current_size -= esz\n        \n        # Prune freq table periodically\n        if len(self.freq) > 60000:\n            in_cache = set(self.cache.keys())\n            extras = [k for k in self.freq if k not in in_cache]\n            for k in extras[:30000]:\n                del self.freq[k]\n        \n        self.cache[obj_id] = (obj_size, f)\n        self.current_size += obj_size\n        return False\n    # EVOLVE-BLOCK-END\n    \n    def hit_rate(self):\n        \"\"\"Return the hit rate as a fraction.\"\"\"\n        total = self.hits + self.misses\n        return self.hits / total if total > 0 else 0.0\n", "changes_description": null, "language": "python", "parent_id": "f0aa29a0-701e-4177-bc24-51a2bc0240cb", "generation": 3, "timestamp": 1780939424.8129878, "iteration_found": 28, "metrics": {"score": 0.513471, "hit_rate": 0.48902, "time": 0.06568264961242676, "combined_score": 0.513471, "artifacts": {"final_size_bytes": 16534528, "final_size_mb": 15.77, "capacity_mb": 16.0, "total_hits": 24451, "total_misses": 25549, "cache_items": 2384, "time_factor": 1.05}}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  # Admission filter: skip one-hit wonders when cache is nearly full\n          if f == 1 and self.current_size > self.capacity * 0.85:\n              return False\n  \n          # Evict: scan front items, pick lowest freq/size score\n          while self.current_size + obj_size > self.capacity and self.cache:\n              best_id = None\n              best_score = float('inf')\n              for i, (cid, (csz, cf)) in enumerate(self.cache.items()):\n                  score = cf / (csz ** 0.25)\n                  if score < best_score:\n                      best_score = score\n                      best_id = cid\n                  if i >= 15:\n                      break\n              if best_id is None:\n                  best_id = next(iter(self.cache))\n              esz, _ = self.cache.pop(best_id)\n              self.current_size -= esz\nwith:\n  # Admission filter: require freq >= 2 for large objects when full\n          if self.current_size > self.capacity * 0.85:\n              if f == 1:\n                  return False\n              if f == 2 and obj_size > self.capacity * 0.01:\n                  return False\n  \n          # Evict: scan front items, pick lowest freq/size score\n          while self.current_size + obj_size > self.capacity and self.cache:\n              best_id = None\n              best_score = float('inf')\n              for i, (cid, (csz, cf)) in enumerate(self.cache.items()):\n                  score = cf / (csz ** 0.15)\n                  if score < best_score:\n                      best_score = score\n                      best_id = cid\n                  if i >= 20:\n                      break\n              if best_id is None:\n                  best_id = next(iter(self.cache))\n              esz, _ = self.cache.pop(best_id)\n              self.current_size -= esz", "parent_metrics": {"score": 0.507549, "hit_rate": 0.48338, "time": 0.07266902923583984, "combined_score": 0.507549, "artifacts": {"final_size_bytes": 16731136, "final_size_mb": 15.96, "capacity_mb": 16.0, "total_hits": 24169, "total_misses": 25831, "cache_items": 2309, "time_factor": 1.05}}, "island": 0}, "prompts": null, "artifacts_json": null, "artifact_dir": null, "embedding": null}