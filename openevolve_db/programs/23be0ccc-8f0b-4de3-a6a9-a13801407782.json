{"id": "23be0ccc-8f0b-4de3-a6a9-a13801407782", "code": "\"\"\"Evolvable cache replacement policy starting from LRU baseline.\"\"\"\n\nfrom collections import OrderedDict\n\n\nclass EvolvedCache:\n    \"\"\"Byte-based cache with evolvable replacement policy.\"\"\"\n    \n    # EVOLVE-BLOCK-START\n    def __init__(self, capacity_bytes):\n        self.capacity = capacity_bytes\n        self.current_size = 0\n        self.hits = 0\n        self.misses = 0\n        # ARC: T1 (recency) + T2 (frequency) + ghost lists B1, B2\n        self.t1 = OrderedDict()  # recently accessed once\n        self.t2 = OrderedDict()  # accessed more than once (hot)\n        self.b1 = OrderedDict()  # ghost of evicted T1 items\n        self.b2 = OrderedDict()  # ghost of evicted T2 items\n        self.t1_size = 0\n        self.t2_size = 0\n        self.t1_sz = {}\n        self.t2_sz = {}\n        self.p = 0  # target bytes for T1\n        self.ghost_max = 3000  # larger ghost for better adaptation\n\n    def _evict_one(self):\n        \"\"\"Evict one item using ARC policy.\"\"\"\n        if self.t1 and (self.t1_size >= max(1, self.p) or not self.t2):\n            eid, _ = self.t1.popitem(last=False)\n            esz = self.t1_sz.pop(eid)\n            self.t1_size -= esz\n            self.current_size -= esz\n            self.b1[eid] = esz\n            if len(self.b1) > self.ghost_max:\n                self.b1.popitem(last=False)\n        elif self.t2:\n            eid, _ = self.t2.popitem(last=False)\n            esz = self.t2_sz.pop(eid)\n            self.t2_size -= esz\n            self.current_size -= esz\n            self.b2[eid] = esz\n            if len(self.b2) > self.ghost_max:\n                self.b2.popitem(last=False)\n        elif self.t1:\n            eid, _ = self.t1.popitem(last=False)\n            esz = self.t1_sz.pop(eid)\n            self.t1_size -= esz\n            self.current_size -= esz\n            self.b1[eid] = esz\n            if len(self.b1) > self.ghost_max:\n                self.b1.popitem(last=False)\n\n    def _evict_to_fit(self, obj_size):\n        \"\"\"Evict until obj_size bytes are free.\"\"\"\n        while self.current_size + obj_size > self.capacity and (self.t1 or self.t2):\n            self._evict_one()\n\n    def access(self, obj_id, obj_size):\n        \"\"\"Process an access request. Returns True for hit, False for miss.\"\"\"\n        # Hit in T1 -> promote to T2\n        if obj_id in self.t1_sz:\n            sz = self.t1_sz.pop(obj_id)\n            self.t1.pop(obj_id)\n            self.t1_size -= sz\n            self.current_size -= sz\n            self.t2[obj_id] = 1\n            self.t2_sz[obj_id] = sz\n            self.t2_size += sz\n            self.current_size += sz\n            self.hits += 1\n            return True\n\n        # Hit in T2 -> move to MRU\n        if obj_id in self.t2_sz:\n            self.t2.move_to_end(obj_id)\n            self.hits += 1\n            return True\n\n        # Miss\n        self.misses += 1\n        if obj_size > self.capacity:\n            return False\n\n        # Ghost hit in B1: favor recency, admit to T2\n        if obj_id in self.b1:\n            ghost_sz = self.b1.pop(obj_id)\n            delta = ghost_sz * max(1, len(self.b2) // max(1, len(self.b1) + 1))\n            self.p = min(self.p + max(1, delta), self.capacity)\n            self._evict_to_fit(obj_size)\n            self.t2[obj_id] = 1\n            self.t2_sz[obj_id] = obj_size\n            self.t2_size += obj_size\n            self.current_size += obj_size\n            return False\n\n        # Ghost hit in B2: favor frequency, admit to T2\n        if obj_id in self.b2:\n            ghost_sz = self.b2.pop(obj_id)\n            delta = ghost_sz * max(1, len(self.b1) // max(1, len(self.b2) + 1))\n            self.p = max(self.p - max(1, delta), 0)\n            self._evict_to_fit(obj_size)\n            self.t2[obj_id] = 1\n            self.t2_sz[obj_id] = obj_size\n            self.t2_size += obj_size\n            self.current_size += obj_size\n            return False\n\n        # Cold miss: admit to T1\n        self._evict_to_fit(obj_size)\n        self.t1[obj_id] = 1\n        self.t1_sz[obj_id] = obj_size\n        self.t1_size += obj_size\n        self.current_size += obj_size\n        return False\n    # EVOLVE-BLOCK-END\n    \n    def hit_rate(self):\n        \"\"\"Return the hit rate as a fraction.\"\"\"\n        total = self.hits + self.misses\n        return self.hits / total if total > 0 else 0.0\n", "changes_description": null, "language": "python", "parent_id": "4584cb33-86d0-4ba4-88d9-127e7a002d6c", "generation": 4, "timestamp": 1780939244.5480013, "iteration_found": 21, "metrics": {"score": 0.512841, "hit_rate": 0.48842, "time": 0.060187578201293945, "combined_score": 0.512841, "artifacts": {"final_size_bytes": 16777216, "final_size_mb": 16.0, "capacity_mb": 16.0, "total_hits": 24421, "total_misses": 25579, "time_factor": 1.05}}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  def __init__(self, capacity_bytes):\n          self.capacity = capacity_bytes\n          self.current_size = 0\n          self.hits = 0\n          self.misses = 0\n          # Segmented LRU with ghost entries for better admission\n          # Protected (hot) ~80%, probationary (new/cold) ~20%\n          self.protected = OrderedDict()   # obj_id -> obj_size (hot items)\n          self.probationary = OrderedDict()  # obj_id -> obj_size (new/cold items)\n          self.protected_size = 0\n          self.probationary_size = 0\n          self.protected_cap = int(capacity_bytes * 0.8)\n          self.freq = {}  # obj_id -> access count\n          # Ghost cache: recently evicted items (no size cost)\n          self.ghost = OrderedDict()  # obj_id -> True, max 1000 entries\n          self.ghost_max = 1000\n  \n      def _evict_to_fit(self, obj_size):\n          \"\"\"Evict items until obj_size bytes are free.\"\"\"\n          while self.current_size + obj_size > self.capacity:\n              if self.probationary:\n                  eid, esz = self.probationary.popitem(last=False)\n                  self.probationary_size -= esz\n                  self.current_size -= esz\n                  # Add to ghost set\n                  self.ghost[eid] = True\n                  if len(self.ghost) > self.ghost_max:\n                      self.ghost.popitem(last=False)\n              elif self.protected:\n                  eid, esz = self.protected.popitem(last=False)\n  ... (64 more lines)\nwith:\n  def __init__(self, capacity_bytes):\n          self.capacity = capacity_bytes\n          self.current_size = 0\n          self.hits = 0\n          self.misses = 0\n          # ARC: T1 (recency) + T2 (frequency) + ghost lists B1, B2\n          self.t1 = OrderedDict()  # recently accessed once\n          self.t2 = OrderedDict()  # accessed more than once (hot)\n          self.b1 = OrderedDict()  # ghost of evicted T1 items\n          self.b2 = OrderedDict()  # ghost of evicted T2 items\n          self.t1_size = 0\n          self.t2_size = 0\n          self.t1_sz = {}\n          self.t2_sz = {}\n          self.p = 0  # target bytes for T1\n          self.ghost_max = 3000  # larger ghost for better adaptation\n  \n      def _evict_one(self):\n          \"\"\"Evict one item using ARC policy.\"\"\"\n          if self.t1 and (self.t1_size >= max(1, self.p) or not self.t2):\n              eid, _ = self.t1.popitem(last=False)\n              esz = self.t1_sz.pop(eid)\n              self.t1_size -= esz\n              self.current_size -= esz\n              self.b1[eid] = esz\n              if len(self.b1) > self.ghost_max:\n                  self.b1.popitem(last=False)\n          elif self.t2:\n              eid, _ = self.t2.popitem(last=False)\n              esz = self.t2_sz.pop(eid)\n  ... (76 more lines)", "parent_metrics": {"score": 0.523173, "hit_rate": 0.49826, "time": 0.09741449356079102, "combined_score": 0.523173, "artifacts": {"final_size_bytes": 16772096, "final_size_mb": 16.0, "capacity_mb": 16.0, "total_hits": 24913, "total_misses": 25087, "protected_items": 1444, "protected_size_mb": 12.79, "time_factor": 1.05}}, "island": 2}, "prompts": null, "artifacts_json": null, "artifact_dir": null, "embedding": null}